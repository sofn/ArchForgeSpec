# Enum sync

1. Change the Java enum.
2. Update `enums/enums.yaml` in the same PR.
3. Update Admin/Web TypeScript constants from the YAML.
4. CI auto-check is Phase 3 backlog; until then reviewers check this list.
