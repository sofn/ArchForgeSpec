# Skill: backend API design

1. Check `api/openapi.yaml` and `specs/api-path.md`.
2. Add/adjust the contract first.
3. Implement Controller with `@SaCheckLogin` and write `@SaCheckPermission`.
4. Use DTOs, not raw Maps.
5. Add a test (unit or RestClient).
6. Update enums.yaml if a shared enum changed.
